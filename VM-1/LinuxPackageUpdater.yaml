---
- hosts: vm-1
  become: yes
  tasks:
    - name: Perform system updates with a delay
      apt:
        upgrade: yes
        update_cache: yes
      async: 300  # Wait for 300 seconds (adjust as needed)
      poll: 0

