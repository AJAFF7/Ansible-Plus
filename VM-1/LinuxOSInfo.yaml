---
- hosts: vm-1  # Replace 'VM' with the name of your target host or group
  become: yes
  tasks:
    - name: Get OS information
      shell: lsb_release -a
      register: os_info
      
    - name: Display OS information
      debug:
        var: os_info.stdout_lines
