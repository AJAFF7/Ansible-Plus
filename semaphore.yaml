version: v1.0
name: MySemaphorePipeline

blocks:
  - name: Build
    task:
      jobs:
        - name: Clone Repository
          commands:
            - checkout:
                git: git@github.com:AJAFF7/Ansible.git
                key: mac_ssh
